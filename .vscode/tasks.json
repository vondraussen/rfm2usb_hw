{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "update images",
            "type": "shell",
            "command": "pcbdraw --silent rfm2usb.kicad_pcb images/rfm2usb_front.png && pcbdraw --silent -b rfm2usb.kicad_pcb images/rfm2usb_back.png"
        },
        {
            "label": "gen docs",
            "type": "shell",
            "command": "kicad-exports -c docs.kibot.yaml"
        }
    ]
}